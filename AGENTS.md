# 项目协作规则

Git 目标：origin / main。普通 push 使用这个明确目标；不擅自换仓库或分支。

## 读取与开发

- 默认先读 PROJECT_STATUS.md；当前对话已读且未变化时不重复读。不默认扫描或总结整个项目。
- README.md 仅在任务需要时读取；代码只读与当前任务直接相关部分，发现依赖问题才扩大范围。
- 优先完成用户要求，每个任务以可独立验收的小功能为宜。简单任务直接做，不写长计划；复杂、高风险或明显有歧义时才做必要分析或询问。用小白能懂的话沟通。
- 优先简单实现，不做过度设计，不重构无关代码，不默认增加文档、测试框架或复杂 Git 流程。
- 尚无项目定义时先索取，不猜测开发；收到后完善 README.md 和 PROJECT_STATUS.md，再开始用户指定的第一项小任务，无需重跑初始化 Skill。

## 完成任务

- 做与本次修改直接相关的必要测试；纯文档做内容检查。无法测试要说明，不声称通过；测试失败先修复，未解决不按成功任务提交。
- 更新 PROJECT_STATUS.md，只保留当前成果、正在做的事、问题和下一步；覆盖旧状态，不追加日志或完整历史。
- 完整小任务完成后创建一次简洁明确的 commit，再 push 到上述目标；不为每个微小修改单独提交，无改动不造空提交。
- 提交前检查差异和暂存区，只暂存本任务文件或改动块；不夹带用户已有改动，不提交密钥、.env 等敏感数据。推送前检查本地尚未推送的提交；含非本任务、未获授权的提交时先询问，不顺带发布。
- 不丢弃用户已有改动，不强推、不清理或重写历史。鉴权、身份配置、测试、提交或推送受阻时保留本地成果，说明阻塞与所需操作；不绕过保护或反复重试。用户明确要求不提交或不推送时遵从。

## 输出

- 成功只简报：完成内容、测试结果、状态文件是否更新、commit / push 结果。
- 失败只说明关键原因，以及用户需处理什么或下一步；不输出完整总结、长篇代码解释或重复步骤。
